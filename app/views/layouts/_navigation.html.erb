<nav class="navbar navbar-default navbar-inverse shadow-bottom" style="border-radius:0px">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Alpha Blog", home_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Articles", articles_path, style: "color: #66cc33" %></li>
        <li><%= link_to "Users", users_path, style: "color: gold" %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <% if !current_user.admin? %>
                <div class="btn btn-xs bubble-wide bubble btn-danger"><%= current_user.username if !current_user.admin? %></div>
            <% else %>
                <div class="btn btn-xs bubble-wide bubble btn-success">Admin</div>
            <% end %>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "View your profile", user_path(current_user) %></li>
            <li><%= link_to "Edit your profile", edit_user_path(current_user) %></li>
            <li><%= link_to "View all users", users_path() %></li>
            <li><%= link_to "Log out", logout_path, method: :delete %></li>
          </ul>
        </li>
        <% else %>
          <li><a href="/signup">
             <div class="btn btn-xs bubble-wide bubble btn-warning-outline">Sign up</div>
          </a></li>
          <li><a href="/login">
             <div class="btn btn-xs bubble-wide bubble btn-success" style="transform:translateY(-1px)">Log in</div>
          </a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
